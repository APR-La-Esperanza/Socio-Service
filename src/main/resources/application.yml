server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: socio-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-sparkling-fog-aqjfhggu-pooler.c-8.us-east-1.aws.neon.tech/apr_socio_db?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_LBdpGJT3ZQ1j}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
